<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.querymaster">

    <application
        android:allowBackup="true"
        android:icon="@drawable/background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Confirm_Booking"></activity>
        <activity android:name=".Booked_Flights" />
        <activity android:name=".Admin_Act" />
        <activity android:name=".Find_Flights" />
        <activity android:name=".Signup_Page" />
        <activity android:name=".Login_Activity" />
        <activity android:name=".Main_Page" />
        <activity android:name=".Splash_Screen_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>